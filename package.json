{
  "name": "mac-transfer",
  "private": true,
  "version": "1.0.0",
  "description": "LAN folder transfer between two Macs via WebRTC, hosted on GitHub Pages",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "hash-wasm": "^4.12.0",
    "mqtt": "^5.15.2"
  }
}
